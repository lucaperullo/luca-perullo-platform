/**
 * Remotion entry point — registered via remotion.config.ts.
 * Imported by `remotion studio` and `remotion render`.
 */

import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
