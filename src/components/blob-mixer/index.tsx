// Public surface of the blob-mixer module.
// The actual scene lives in `scene.client.tsx` (client component).
export { BlobMixerScene } from "./scene.client";
export { BLOB_PRESETS, BLOB_PRESET_BY_ID, type BlobPreset } from "./presets";
